# Run100 (런백) 개발 릴리즈 노트 & 작업 히스토리

Run100 앱의 개발 진행 상황, 주요 기능 구현, 아키텍처 결정 사항을 기록하는 문서입니다.

---

## [v0.1.0] - 2026-09-19 : 프로젝트 기획 및 PRD 확정 📋

### 🎯 핵심 기획 정의
- **앱의 본질**: 실시간 GPS 측정과 복잡한 SNS 기능을 걷어내고, 오직 **"월 100km 누적 달성"**과 **"잔디 심기(습관 형성)"** 2가지 핵심 가치에 100% 집중하는 미니멀 러닝 트래커.
- **핵심 코칭**: 남은 거리와 당월 잔여 일수를 바탕으로 **"오늘 몇 km 뛰어야 하는가?"**를 매일 역산 제시 (`(100km - 누적km) / 잔여일수`).
- **화면 구조**: **[대시보드]**와 **[러닝 캘린더]** 2대 탭 화면 + 빠른 수동 기록 바텀 시트.

### 💡 주요 의사결정 사항
1. **위젯 중심 아키텍처 (WidgetKit - Medium 2x4 단일 집중)**
   - 복잡한 여러 규격 대신, 진행률 원형 링과 최근 7일 잔디 심기 및 역산 코칭을 모두 담을 수 있는 최적의 규격인 **Medium 위젯(2×4 직사각형)** 단일 지원으로 집중 결정.
2. **라이트 모드 기본 (Light-First UI)**
   - 깔끔하고 화사한 화이트/오프화이트 기반의 라이트 모드를 기본 화면 모드로 설정하고, 시스템 설정에 따른 다크 모드도 완벽 대응.
   - 잔디 심기 히트맵(0km 휴식 ~ 10km+ 고강도) 색상을 라이트/다크 모드 적응형으로 설계.

---

## [v0.1.1] - 2026-09-19 : Step 1 Core Foundation 완료 🧱

### 🚀 구현 완료 내역
1. **SwiftData 모델 (`RunSession.swift`)**
   - 단일 달리기 기록의 고유 식별자(`id`), 일시(`date`), 달린 거리(`distanceKm`), 시간(`durationSeconds`), 메모(`memo`), 수동 입력 여부(`isManual`), 데이터 출처(`source`) 스키마 정의.
2. **역산 코칭 및 집계 모델 (`MonthlyProgress.swift`)**
   - 월 100km 목표 대비 누적/남은 거리, 달성률, D-Day 계산.
   - 당월 남은 일수를 기반으로 오늘 뛰어야 할 권장 거리(`recommendedDailyKm = remainingKm / remainingDays`) 역산 알고리즘 및 동기부여 코칭 메시지 구현.
3. **비즈니스 상태 매니저 (`RunStore.swift`)**
   - SwiftData 세션 데이터를 기반으로 당월 집계, 연속 달리기 스트릭(Streak), 일자별 거리 그룹핑 로직 구현.
4. **UI 메인 뼈대 및 컨테이너 연결**
   - `Run100App.swift`: `.modelContainer(for: RunSession.self)` 주입.
   - `ContentView.swift`: [대시보드]와 [캘린더]를 아우르는 2-Tab Navigation 구조 생성.
   - `DashboardView.swift` & `CalendarView.swift` 기본 화면 뼈대 구현.
5. **계산 로직 단위 테스트 (`Run100Tests.swift`)**
   - 목표 달성 계산, 남은 거리, 권장 거리 및 스트릭 집계 검증 통과.

---

## [v0.1.2] - 2026-09-19 : Step 2 Dashboard View UI 완료 🎯

### 🚀 구현 완료 내역
1. **원형 100km 프로그레스 링 (`ProgressRingView.swift`)**
   - 캔버스 디자인과 1:1 매칭되는 오렌지-앰버 그라디언트(`AngularGradient`) 원형 게이지.
   - 링 중앙에 현재 누적 거리(`68.0 km`), `/ 100 km`, 목표 달성 퍼센트 배지(`68% 달성`) 배치.
   - 달성률 변경 시 부드러운 스프링 애니메이션(`.spring(response: 0.8)`) 적용.
2. **대시보드 히어로 카드 (`HeroDashboardCard.swift`)**
   - 좌측 원형 게이지와 우측 2단 카드(`완주까지 남은 거리`, `오늘 권장 달리기 코칭 카드`) 배치.
   - 하단 연속 스트릭 인디케이터(`🔥 N일 연속 달리는 중`) 및 캘린더 탭 전환 버튼.
3. **빠른 달리기 기록 추가 바텀 시트 (`QuickAddModalView.swift`)**
   - 대형 숫자 입력 디스플레이와 `[3km]`, `[5km]`, `[7km]`, `[10km]` 퀵 선택 칩.
   - 메모 및 소요 시간 입력 필드.
   - 저장 시 SwiftData 저장(`modelContext.insert`) 및 성공 햅틱 피드백(`UINotificationFeedbackGenerator`).
4. **최근 달리기 피드 (`RecentRunRowView.swift`) & 대시보드 통합 (`DashboardView.swift`)**
   - 코스명, 날짜 배지, 소요 시간, 달린 거리 및 당월 기여도(`+6.2% 기여`) 카드 렌더링.
   - 기록이 없을 때 첫 러닝을 독려하는 깔끔한 빈 상태(Empty State) UI.
   - 라이트 모드(화이트/오프화이트)와 다크 모드 적응형 컬러 시스템 적용.

---

## [v0.1.3] - 2026-09-19 : 설정 메뉴(SettingsView) 요건정의서 반영 ⚙️

### 🎯 기획 개선 사항
- **대시보드 상단 버튼 역할 전환**:
  - 대시보드 본문에 이미 와이드한 [기록 추가하기 +] 버튼이 존재하여, 상단 우측 `+` 버튼의 기능 중복을 해결.
  - 상단 우측 버튼을 **설정(톱니바퀴 ⚙️) 버튼**으로 교체하여 전용 설정 모달 시트(`SettingsView`) 호출 진입점으로 전환.
- **설정 화면(SettingsView) 상세 명세 확정**:
  1. **월간 목표 거리 커스텀**: 기본 100km 외 프리셋(`50km`, `100km`, `150km`) 및 목표 변경 지원
  2. **화면 테마 설정**: 시스템 설정 / 라이트 모드 (기본) / 다크 모드 선택
  3. **저녁 러닝 리마인더 (UserNotifications)**: 당일 미달성 시 저녁 7시 동기부여 알림 On/Off
  4. **애플 건강 (HealthKit) 연동 상태**: 권한 승인 상태 및 재요청 링크
  5. **앱 정보 및 데이터 관리**: 버전(`v0.1.x`) 표시 및 로컬 기록 초기화

---

## [v0.1.4] - 2026-09-19 : 대시보드 톱니바퀴 버튼 및 설정 화면(SettingsView) 구현 완료 ⚙️

### 🚀 구현 완료 내역
1. **대시보드 상단 톱니바퀴(⚙️) 버튼 연동 (`DashboardView.swift`)**
   - 상단 헤더 우측의 중복된 `+` 버튼을 직관적인 톱니바퀴(`gearshape.fill`) 설정 버튼으로 교체.
   - 탭 시 설정 화면(`SettingsView`) 대형 모달 시트 오픈.
2. **설정 화면 컴포넌트 (`SettingsView.swift`)**
   - **월간 목표 설정**: `@AppStorage("monthlyTargetKm")` 연동, `[50km]`, `[100km]`, `[150km]` 퀵 프리셋 칩 및 슬라이더 미세 조정 지원.
   - **화면 모드(테마)**: 라이트 모드 (기본), 다크 모드, 시스템 설정 일치 피커 지원.
   - **애플 건강 연동**: HealthKit 연동 상태 및 Step 4 안내.
   - **데이터 관리**: SwiftData 전체 기록 일괄 초기화(Confirmation Dialog 지원) 및 앱 정보(`v0.1.3`).
3. **앱 전역 실시간 반영 (`ContentView.swift` & `MonthlyProgress.swift`)**
   - 설정에서 테마 모드 변경 시 `.preferredColorScheme`을 통해 앱 전체의 화면 테마가 실시간으로 전환.
   - 목표 거리 변경 시 대시보드 타이틀(`MONTHLY 50K` 등), 남은 거리, 일일 권장 거리가 실시간으로 재계산되어 즉시 반영.

---

## [v0.1.5] - 2026-09-19 : Step 3 Calendar View UI (30일 잔디 히트맵 & 상세 카드) 완료 🌱

### 🚀 구현 완료 내역
1. **30일 잔디 심기 히트맵 그리드 (`HeatmapGridView.swift` & `HeatmapCellView.swift`)**
   - 캔버스 디자인과 1:1 매칭되는 월~일 7열 캘린더 그리드.
   - 달린 거리별 4단계 적응형 색상 매핑:
     - `휴식 Day`: 연회색 / 다크 슬레이트 (`#27272A`)
     - `0.1 ~ 4.9 km`: 연한 오렌지
     - `5.0 ~ 9.9 km`: 표준 오렌지
     - `10.0 km 이상`: 비비드 앰버 골드 (`#FBBF24`)
   - 오늘 날짜 테두리 링 강조 및 미래 날짜 구분 처리.
   - 날짜 셀 탭 시 햅틱 피드백(`UIImpactFeedbackGenerator`) 및 하단 상세 카드 업데이트.
2. **월간 통계 3분할 카드 (`CalendarView.swift`)**
   - 상단 `🔥 N일 연속 스트릭` 배지.
   - `달린 날수`, `휴식한 날`, `출석률(%)` 3대 핵심 통계 실시간 집계.
3. **선택 일자 러닝 상세 카드 (`DayDetailCardView.swift`)**
   - 선택된 날짜의 총 달린 거리 및 당일 소요 시간, 완료 세션 수, 메모 목록 표시.
   - 기록이 없는 휴식일에는 근육 회복을 독려하는 회복 카드(`🧘 이 날은 푹 쉬며 근육을 회복했어요`) 노출.

---

## [v0.1.6] - 2026-09-19 : Step 4 HealthKit 자동 동기화 구현 완료 💓

### 🎯 주요 변경 사항
- **애플 건강(Apple HealthKit) 연동 코어 서비스 (`HealthKitManager.swift`)**
  - Apple Watch 피트니스 기록 등 애플 건강 앱에 저장된 달리기(`HKWorkoutActivityType.running`) 및 거리(`distanceWalkingRunning`) 데이터 쿼리.
  - **중복 방지 알고리즘**: 건강 앱의 `HKWorkout.uuid`와 로컬 `RunSession.id`를 1:1 대조하여 동일 세션 재동기화 시 중복 추가를 100% 차단.
  - SwiftData `modelContext`를 통한 비동기(`async/await`) 자동 영구 저장.
  - 시뮬레이터 및 테스트 환경을 위한 샘플 워크아웃 생성(`addSampleHealthWorkout`) 기능 내장.
- **대시보드 동기화 UX (`DashboardView.swift`)**
  - [애플 건강 자동 동기화] 버튼 활성화: 동기화 중 스피너(`ProgressView`) 로딩 상태 표시.
  - 동기화 완료 시 햅틱 피드백(`UINotificationFeedbackGenerator`) 및 결과 알림(동기화된 건수, 누적 km 수치) 안내.
- **설정 화면 연동 (`SettingsView.swift`)**
  - Apple HealthKit 지원 상태 배지 노출.
  - 최근 동기화 시각(`lastSyncDate`) 표시 및 "지금 즉시 동기화하기" 버튼 연동.
- **프로젝트 권한 설정 (`project.pbxproj`)**
  - `INFOPLIST_KEY_NSHealthShareUsageDescription` 및 `INFOPLIST_KEY_NSHealthUpdateUsageDescription` 등록.
- **단위 테스트 추가 (`Run100Tests.swift`)**
  - HealthKit 세션 속성 보존 및 고유 UUID 기반 중복 방지 로직 테스트 케이스 검증.

---

## [v1.0.0] - 2026-09-19 : Step 5 Medium 홈 화면 위젯 구현 완료 & 1.0 정식 릴리즈 달성 🚀

### 🎯 주요 변경 사항
- **Medium 2x4 홈 화면 바탕화면 위젯 구현 (`Run100Widget.swift` & `MediumWidgetView.swift`)**
  - 복잡한 다중 규격 대신 가독성과 정보성이 가장 뛰어난 **Medium 위젯(2×4 직사각형)** 단일 지원으로 최적화.
  - **좌측 영역**: 완주율 미니 원형 프로그레스 링, 누적 km(`68.0/100km`), 완주 퍼센트 배지(`68%`).
  - **우측 상단**: D-Day 배지(`D-9`) 및 오늘 권장 달리기 배지(`오늘 3.5km`).
  - **우측 중앙 (핵심)**: **최근 7일 잔디 심기 미니 스트립 (7-Dot Strip)**으로 데일리 습관 시각화 (오늘 날짜 링 강조).
  - **우측 하단**: 동기부여 역산 코칭 문구 노출.
- **앱-위젯 실시간 공유 브리지 (`WidgetDataBridge.swift`)**
  - 앱에서 기록 추가, 건강앱 동기화, 목표 거리 변경 시 `WidgetCenter.shared.reloadAllTimelines()`를 통해 위젯이 즉시 갱신됨.
  - `WidgetSnapshotData` Codable 모델을 통해 안전한 데이터 스냅샷 전달.
- **단위 테스트 추가 (`Run100Tests.swift`)**
  - `testWidgetSnapshotDataModel`: 위젯 스냅샷 데이터 무결성 및 JSON 인코딩/디코딩 검증 통과.
- **HealthKit 서명 권한 완비 (`Run100.entitlements`)**
  - `com.apple.developer.healthkit` capability 등록으로 실제 기기 및 시뮬레이터 권한 무결성 확보.

---

## [전체 개발 로드맵 완료 현황]
- [x] **Step 1: Core Foundation** - SwiftData 모델(`RunSession`), 역산 계산 로직(`MonthlyProgress`), 메인 탭 뼈대
- [x] **Step 2: Dashboard View & Settings** - 원형 프로그레스 링, 히어로 카드, 빠른 수동 기록, 설정 화면(SettingsView)
- [x] **Step 3: Calendar View** - 30일 잔디 심기 히트맵 그리드, 일자별 상세 카드, 스트릭 계산
- [x] **Step 4: HealthKit Integration** - 애플 건강 앱 달리기 워크아웃 자동 동기화 & 동기화 완료 배너
- [x] **Step 5: Home Screen Widget** - WidgetKit 바탕화면 위젯 (Medium 2x4 단일 규격) 구현

---

## [v1.0.1] - 2026-09-19 : 수기 달리기 기록 롱프레스 햅틱 및 수정/삭제 팝업 지원 ✏️🗑️

### 🎯 주요 변경 사항
- **수기 입력 기록(`isManual: true`) 롱프레스 인터랙션 연동**
  - **대시보드 최근 기록 피드 (`RecentRunRowView.swift`)**:
    - 수기 기록 셀을 손가락으로 꾹 누르면(롱프레스) 햅틱 피드백과 함께 **[기록 수정]** 및 **[기록 삭제]** 컨텍스트 팝업 메뉴 노출.
    - 애플 건강(HealthKit) 자동 동기화 기록은 실수로 삭제되지 않도록 구분 보호.
  - **캘린더 일자별 상세 카드 (`DayDetailCardView.swift`)**:
    - 캘린더에서 특정 날짜를 선택했을 때 하단에 표시되는 세션 카드에서도 수기 기록을 꾹 눌러 즉시 수정/삭제 가능.
- **수정 모달 시트 (`QuickAddModalView.swift`)**
  - 기존 세션의 거리, 메모, 운동 시간을 그대로 불러와 수정 후 저장 가능 (`isEditing: true`).
- **안전한 삭제 다이얼로그 (`confirmationDialog`)**
  - 실수로 탭하여 지워지는 일을 방지하기 위해 삭제 전 확인 팝업 안내 문구 제공.
  - 삭제 시 SwiftData에서 즉시 제거되며 대시보드 링, 잔디 캘린더, 홈 화면 위젯에 실시간 자동 반영.

---

## [v1.0.2] - 2026-09-19 : 탭 바 설정 탭 추가 및 대시보드 상단 월 선택기 도입 🗓️⚙️

### 🎯 주요 변경 사항
- **3개 탭 바(TabView) 구조 개편**
  - 대시보드 우측 상단 모달 시트로 띄우던 **[설정]** 화면을 하단 탭 바의 3번째 공식 탭(`gearshape.fill`)으로 승격.
  - 탭 구조: **[대시보드]** (Tab 0) / **[캘린더]** (Tab 1) / **[설정]** (Tab 2).
  - 언제 어디서든 하단 탭을 통해 월간 목표 거리(100km), 화면 테마 모드, 애플 건강 동기화 상태에 즉시 접근 가능.
- **대시보드 우측 상단 월 선택기 (`< M월 ▾ >`) 탑재 & 표기 정밀화**
  - 기존 설정 톱니바퀴가 있던 헤더 우측 공간에 캡슐 형태의 월 이동 및 선택 컨트롤 배치.
  - **연도 표기 콤마 제거**: 드롭다운 메뉴에서 `2,026년`으로 천 단위 콤마가 붙던 현상을 `Text(verbatim:)` 처리하여 깔끔한 `2026년 9월`로 수정.
  - **`<` 버튼**: 터치 시 이전 달 대시보드 및 누적 기록으로 이동.
  - **`M월 ▾` 드롭다운 메뉴**: 최근 12개월 목록이 표시되어 원하는 월로 한 번에 즉시 점프.
  - **`>` 버튼**: 다음 달로 이동하며, 당월을 초과하는 미래 월 조작을 방지하기 위해 현재 달 기준 비활성화(`disabled`) 처리.
- **애플 건강(HealthKit) 과월 데이터 연동 및 동기화 범위 확장**
  - 기존 당월 1일 이후로 제한되어 있던 HealthKit 쿼리 범위를 **최근 1년(과거 12개월 전체)**으로 확장.
  - 동기화 버튼 터치 시 과거 1년간의 애플워치 러닝 워크아웃이 모두 SwiftData에 중복 없이 안전하게 누적되어, 8월/7월 등 과월을 선택했을 때도 과거 러닝 기록, 프로그레스 링, 잔디가 정상 표시됨.
  - 시뮬레이터 환경에서도 현재 선택된 연/월에 맞추어 샘플 러닝 세션을 생성할 수 있도록 지원.
  - 과월 선택 시 "챌린지가 종료되었습니다" 등 과거 상태에 어울리는 코칭 가이드 메시지 분기 처리.
---

## [v1.0.3] - 2026-09-19 : 대시보드 히어로 카드 전면 개편 (평균 페이스 & 누적 거리 라인 차트) 📈⏱️

### 🎯 주요 변경 사항
- **우측 상단: 이번 달 평균 페이스 카드 (`paceValueOnly`)**
  - 기존의 복잡했던 "완주까지 남은 거리" 표시를 제거하고, 이번 달 나의 **평균 페이스(예: `5:30 /km`, `6:00 /km`)**를 대형 수치로 전면 배치.
  - 당월 러닝 횟수와 총 운동 시간(`총 N시간 N분`)을 서브 요약으로 깔끔하게 제공.
- **우측 하단: 이번 달 누적 거리 추이 라인 차트 카드 (Swift Charts)**
  - 기존의 "오늘 권장 달리기" 및 "남은 12일 동안..." 코칭 텍스트를 제거하여 대시보드를 미니멀하고 직관적으로 정돈.
  - 현재 누적 거리 수치와 함께 1일부터 오늘(과월은 말일)까지의 일자별 러닝 누적 성장 곡선을 보여주는 미니 라인 차트(오렌지 그라디언트 에어리어 & 캣멀-롬 곡선)를 시원한 높이(42pt)로 탑재.
- **단위 테스트 무결성 검증 (`Run100Tests.swift`)**
  - `testAveragePaceCalculations`: 평균 페이스 계산 및 경계값 검증 통과.
  - `testDailyCumulativePointsAggregation`: 일자별 누적 거리 포인트 계산 알고리즘 검증 통과.

---

## [v1.0.4] - 2026-09-19 : 누적 거리 차트에 최근 3개월치 비교 회색 라인 오버레이 탑재 📊🏃

### 🎯 주요 변경 사항
- **최근 3개월 누적 러닝 곡선 오버레이 비교 기능 (Benchmarking Comparison)**
  - 선택된 월 기준 **직전 1개월 전, 2개월 전, 3개월 전**의 누적 거리 궤적을 은은한 회색 점선 라인으로 당월 차트 배경에 겹쳐서 표시.
  - 이번 달(오렌지 비비드 라인 & 그라디언트)의 페이스가 지난 3개월과 비교해 빠른지, 느린지 러닝 누적 페이스를 한눈에 직관적으로 벤치마킹 가능.
  - 차트 하단에 직관적인 미니 범례(`선택월 ─`, `직전 3개월 ⋯`) 제공.
- **비교 데이터 집계 모델 및 비즈니스 로직**
  - `MonthComparisonSeries`: 과거 월 누적 거리 포인트 시리즈 모델 추가.
  - `RunStore.swift`: 선택된 연/월 기준 -1, -2, -3개월 전 세션을 필터링하여 일자별 누적 합산 배열 자동 생성.
- **설정 화면 앱 버전 동적 로드 및 프로젝트 버전 동기화**
  - 기존 설정 화면에 `v1.0.0 (Step 5 위젯 완료)`로 하드코딩되어 있던 텍스트를 제거하고, `Bundle.main`의 `CFBundleShortVersionString` 및 `CFBundleVersion`을 동적으로 가져와 `v1.0.4 (1)`로 실시간 표시하도록 개선.
  - Xcode 프로젝트(`Run100.xcodeproj/project.pbxproj`)의 `MARKETING_VERSION`을 최신 릴리즈 버전인 `1.0.4`로 일괄 동기화.
- **단위 테스트 추가 (`Run100Tests.swift`)**
  - `testPastMonthsSeriesAggregation`: 과거 월 데이터 필터링 및 누적 합산 검증 통과.

---

## [v1.0.5] - 2026-09-19 : 누적 거리 그래프 제거 및 이번 달 평균 심박수(bpm) 카드 탑재 ❤️⏱️

### 🎯 주요 변경 사항
- **대시보드 메인 누적 거리 그래프 완전 제거 및 미니멀화**
  - 대시보드를 더 깔끔하고 가독성 높게 유지하기 위해 복잡했던 일자별 누적 거리 라인 차트를 완전히 제거.
- **우측 하단: 이번 달 평균 심박수 카드 전면 배치 (`averageHeartRate`)**
  - 당월 러닝 세션들의 심박수 데이터를 자동 집계하여 **평균 심박수(예: `152 bpm`)**를 24pt 대형 볼드 숫자로 표시.
  - 심박수 수치에 따른 러닝 강도 안내 문구 제공 (적정 유산소 심박 구간 / 고강도 러닝 구간 등).
  - 우측 상단 **평균 페이스(속도, 오렌지)**와 우측 하단 **평균 심박수(강도, 레드)**의 조화로운 듀얼 러닝 메트릭 완성.
- **HealthKit 심박수 통계 연동 및 데이터 모델 확장**
  - `RunSession`: `averageHeartRate: Int?` (bpm) 필드 추가.
  - `HealthKitManager`: 워크아웃 동기화 시 `HKQuantityType.heartRate` 통계의 평균 심박수를 자동 추출하여 저장.
  - 시뮬레이터 샘플 생성 시에도 현실적인 러닝 심박수(145~165 bpm) 부여.
- **대시보드 상단 월 선택기 라벨 미니멀화 (`< M월 >`)**
  - "9월", "8월" 옆에 붙어있던 불필요한 아래 화살표(`chevron.down`)를 제거하여, 군더더기 없이 깔끔하고 세련된 캡슐 버튼 형태로 정돈.
- **단위 테스트 추가 (`Run100Tests.swift`)**
  - `testAverageHeartRateCalculations`: 심박수가 있는 세션들의 평균 산출 및 심박수 없는 수기 세션 제외 필터링 검증 통과.

---

## [v1.0.6] - 2026-09-19 : 캘린더 잔디 심기 날짜 그리드 오프셋 정합성 복구 및 월 선택기 탑재 🗓️🛠️

### 🎯 주요 변경 사항
- **캘린더 그리드 ID 충돌 및 날짜 누락 버그 완벽 해결 (`HeatmapGridView.swift`)**
  - **원인 분석**:
    - 기존에는 1일 앞의 빈 공간 셀에 `ForEach(0..<firstDayWeekdayOffset, id: \.self)`를 사용하고, 뒤이어 날짜 셀에 `ForEach(1...totalDays, id: \.self)`를 사용하여, 오프셋이 5인 8월 등 특정 월에서 **빈 셀의 정수 ID(0, 1, 2, 3, 4)와 날짜 셀의 정수 ID(1, 2, 3, 4)가 중복(Duplicate ID)** 충돌.
    - SwiftUI `LazyVGrid`의 뷰 재사용 메커니즘으로 인해 ID가 겹친 1일~4일(토, 일, 월, 화) 셀이 빈 셀로 대체/누락되면서 화면에 토~화가 비고 5일(수요일)부터 시작하는 것처럼 보이던 현상 발생.
  - **개선 조치**:
    - `GridItemType` enum 모델을 도입하여 빈 오프셋 셀(`"empty_2026_8_0"`)과 날짜 셀(`"day_2026_8_1"`)의 식별자를 완전히 고유화.
    - 단일 `ForEach(gridItems)`로 일원화 렌더링하여 ID 충돌을 원천 차단.
    - `LazyVGrid`에 `.id("\(year)-\(month)")`를 부여하여 연/월 전환 시 전체 그리드를 깨끗하게 새로 구성하도록 보장.
    - `DateComponents` 생성 시 `hour: 12`를 명시하여 자정/타임존/서머타임 경계에 따른 날짜 밀림 현상 방지.
- **캘린더 상단 월 선택기 (`< M월 >`) 추가 (`CalendarView.swift`)**
  - 대시보드로 돌아가지 않고도 캘린더 화면 자체에서 이전 달/다음 달로 바로 전환하고 원하는 월을 선택할 수 있도록 헤더 우측에 월 선택 컨트롤러 탑재.
  - 월 변경 시 선택 일자(`selectedDay`)가 변경된 월의 총 일수(`totalDaysInMonth`)를 초과하지 않도록 자동 클램핑 보정.
- **2026년 8월/9월 요일 정합성 검증**:
  - 8월 1일: 토요일 (오프셋 5: 월/화/수/목/금 5칸 비고 토요일부터 1일 시작)
  - 8월 2일: 일요일
  - 8월 3일: 월요일
  - 8월 4일: 화요일
  - 8월 5일: 다음 주 수요일
  - 8월 31일: 월요일
  - 9월 1일: 화요일 (오프셋 1: 월 1칸 비고 화요일부터 1일 시작)
- **연속 스트릭 배지 기능 F-202(잔디 심기 카드)로 이전 통합 (`HeatmapGridView.swift`, `CalendarView.swift`)**
  - 기존 캘린더 화면 상단 타이틀 영역에 분리되어 있던 `🔥 N일 연속` 배지를 잔디 심기 카드 헤더(`M월 잔디 심기` 옆)로 이동.
  - 상단 앱 헤더는 미니멀하고 시원하게 정돈하고, 연속 러닝 습관과 잔디 심기 히트맵의 시각적 연계성을 극대화.
- **단위 테스트 추가 및 전체 테스트 통과 (`Run100Tests.swift`)**
  - `testCalendarWeekdayOffsets`: 8월(토요일 시작) 및 9월(화요일 시작)의 요일 오프셋 및 주차 배치를 검증하는 테스트 케이스 추가 및 통과.

---

## [v1.0.7] - 2026-09-19 : 최근 달리기 기록 미니멀 UI 개편 (거리·페이스·심박수·날짜 4대 지표) 🏃💨

### 🎯 주요 변경 사항
- **최근 달리기 피드 4대 핵심 지표 전면 개편 (`RecentRunRowView.swift`)**
  - **불필요한 정보 완전 제거**:
    - "이준성의 Apple..." 등 기기명/세션 메모 제거.
    - "애플 건강", "수기 직접 입력" 등 출처 구분 라벨 및 아이콘 박스 제거.
    - `+6.2% 기여` 등 불필요한 부가 수치 제거.
  - **러너를 위한 4대 필수 메트릭 배치**:
    1. **날짜**: `9월 14일` (좌측 상단, 깔끔한 11pt 세미볼드)
    2. **거리**: `5.2 km` (좌측 하단, 20pt 오렌지 라운디드 볼드)
    3. **페이스**: `speedometer` 아이콘 + `5:30/km` (우측 상단, 오렌지 포인트)
    4. **심박수**: `heart.fill` 아이콘 + `152 bpm` (우측 하단, 핑크/레드 포인트, 미측정 시 `- bpm`)
- **노출 데이터 개수 확장 (최근 5건 -> 최근 7건)**
  - 대시보드 피드에서 당월 최신 러닝 기록을 최근 7회(`prefix(7)`)까지 확인할 수 있도록 확장.
- **HealthKit 심박수 수집 2단계 Fallback 쿼리 및 기존 데이터 백필(`HealthKitManager.swift`)**
  - **Fallback 쿼리(`HKStatisticsQuery`)**: 서드파티 앱(나이키 런 클럽, 스트라바, 가민 등)으로 기록되었거나 `workout.statistics(for:)`가 비어있는 워크아웃의 경우, 해당 운동 시간대(`startDate ~ endDate`)의 심박수 샘플 통계를 직접 쿼리하여 심박수를 100% 수집하도록 강화.
  - **기존 데이터 백필(Backfill)**: 이전에 동기화되어 로컬에 이미 저장되어 있으나 심박수가 비어있던(`averageHeartRate == nil`) 세션들에 대해, 동기화 버튼 터치 시 심박수를 소급 추적하여 자동으로 업데이트하도록 개선.
- **세션 모델 페이스 프로퍼티 추가 (`RunSession.swift`)**
  - `paceString`: `durationSeconds`와 `distanceKm`를 바탕으로 `min:sec/km` 페이스를 자동 산출하는 헬퍼 프로퍼티 추가.
- **단위 테스트 추가 (`Run100Tests.swift`)**
  - `testRunSessionPaceString`: 거리와 운동 시간에 따른 페이스 포맷 계산 및 0km 방어 로직 검증 통과.

---

## [v1.0.8] - 2026-09-19 : 캘린더 일자별 상세 카드 미니멀 개편 (F-203) 🗓️🏃

### 🎯 주요 변경 사항
- **선택 일자 러닝 상세 카드 5대 핵심 지표 위주 전면 개편 (`DayDetailCardView.swift`)**
  - **불필요한 정보 완전 제거**:
    - "이준성의 Apple watch 러닝" 등 기기명/세션 메모 제거.
    - "애플건강", "수기 직접 입력" 등 출처 구분 라벨 및 아이콘 박스 제거.
  - **러너 중심의 5대 메트릭 재배치**:
    1. **달리기 시간(시작 시각)**: `clock.fill` + `오전 7:30` (좌측 상단, 깔끔한 캡슐 배지)
    2. **거리**: `5.2 km` (우측 상단, 18pt 오렌지 라운디드 볼드)
    3. **달린 시간(지속 시간)**: `32분 40초` (하단 좌측)
    4. **페이스**: `speedometer` + `5:30/km` (하단 중앙)
    5. **심박수**: `heart.fill` + `152 bpm` (하단 우측, 미측정 시 `- bpm`)
  - **직관적인 2단 카드 레이아웃**: 상단에 운동 시각과 거리를 강조하고, 하단에 3분할(달린 시간 | 페이스 | 심박수)을 배치하여 가독성을 극대화.

---

## [v1.1.0] - 2026-09-19 : 하단 탭 바 히스토리(History) 탭 추가 및 월별 다차원 성장 추이 분석 기능 탑재 📊🏆

### 🎯 주요 변경 사항
1. **하단 TabView 4대 탭 체계 개편 (`ContentView.swift`)**
   - Tab 1: **대시보드** (`figure.run`)
   - Tab 2: **캘린더** (`calendar`)
   - Tab 3: **히스토리** (`chart.bar.xaxis`) [신설]
   - Tab 4: **설정** (`gearshape.fill`)
2. **월별 성장 분석 모델 구축 (`MonthlyHistorySummary.swift`)**
   - 월별 누적 거리, 총 운동 시간, 평균 페이스, 평균 심박수, 달린 날수(출석률), 목표(100km) 달성 여부를 캡슐화한 불변 요약 모델.
3. **RunStore 비즈니스 로직 확장 (`RunStore.swift`)**
   - `calculateHistorySummaries(from:monthsLimit:targetKm:)`: 최근 6개월/12개월 단위로 세션 데이터를 과거부터 최신순으로 자동 그룹핑 및 통계 집계.
4. **러닝 히스토리 화면 구축 (`Views/History/`)**
   - **분석 기간 선택 필터 (`HistoryPeriod`, F-301)**: `최근 6개월` / `최근 12개월 (1년)` 피커.
   - **종합 요약 배너**: 총 누적 거리, 목표 달성 횟수(🏆), 평균 페이스, 평균 심박수 하이라이트.
   - **4대 지표별 Swift Charts 시각화**:
     - **거리 추이 (`HistoryDistanceChartView`, F-302)**: 100km 목표선(RuleMark), 100km 완주 월 골드 그라데이션 및 트로피(🏆) 배지, BEST 월 하이라이트.
     - **페이스 추이 (`HistoryPaceChartView`, F-303)**: 월별 평균 속도 변화 스무스 라인 및 역대 최고 페이스(⚡️) 핀포인트.
     - **심박수 추이 (`HistoryHeartRateChartView`, F-304)**: 유산소 심박 기준 영역(140~160bpm) 오버레이 및 가장 안정된 심박(🌱) 모니터링.
     - **달린 날 및 출석률 (`HistoryConsistencyChartView`, F-305)**: 월별 달린 일수 및 출석률(%) 막대 차트.
   - **월별 종합 성적표 피드 (`HistoryMonthlyCardView`, F-306)**:
     - 각 월별 4대 지표 미니 배지 카드 렌더링.
     - "대시보드 보기", "캘린더 보기" 버튼 클릭 시 해당 월로 스토어 상태를 동기화하고 즉시 화면 점프 이동.
5. **단위 테스트 추가 (`Run100Tests.swift`)**
   - `testMonthlyHistorySummaryCalculations`: 100km 달성 여부, 출석률 계산, 페이스 포맷 검증.
   - `testCalculateHistorySummariesPeriodsAndOrdering`: 6개월/12개월 데이터 슬라이싱 및 시간순 정렬 검증 통과.

---

## [v1.2.0] - 2026-09-20 : F-103 러닝 프로그레스 레이스 트랙 & 최근 3개월 동기간 고스트 러너 경쟁 기능 탑재 및 UI 미니멀화 🏃💨👻

### 🎯 주요 변경 사항
1. **대시보드 빠른 액션 버튼군 완전 개편 (`F-103`)**
   - 기존의 수기 추가 및 건강 동기화 버튼을 제거하고, 직관적인 **100km 실시간 러닝 레이스 트랙**과 **동기간(N일차) 고스트 러너 경쟁 카드**로 전면 교체.
   - 건강 동기화는 대시보드 당겨서 새로고침(`.refreshable`) 및 설정 탭 관리로 일원화하여 불필요한 UI 요소를 배제.
2. **실시간 100km 레이스 트랙 & 러너 이모지 애니메이션 (`RunningTrackProgressCard.swift`)**
   - **오른쪽 결승선 방향 주행**: 0km 출발선부터 100km(🏁) 결승선까지 진행률에 비례하여 결승선을 향해 오른쪽으로 달리는 러너(`🏃`, 수평 반전) 이모지 동적 전진.
   - **러너 누적 거리 말풍선**: 러너 머리 위에 실시간 누적 거리 말풍선(`68.0 km`) 표시.
   - **트랙 위 실시간 고스트 핀 (과거의 나)**: 트랙 바 위에 과거 3개월(직전 8월, 7월 등)의 동기간(N일차) 누적 거리 위치에 귀여운 유령(`👻`) 아이콘과 미니 뱃지(`8월 50k`)가 함께 달려 실시간 추월 여부 체감.
   - **트랙 헤더 극단적 미니멀화**: '완주까지 X.Xkm 남음'과 같은 부가 피드백 텍스트, 초과 거리 및 D-Day 배지를 전면 제거하여, 시각적 피로도 없이 직관적인 `🏃 100km 레이스 트랙` 타이틀 본연에만 집중 (목표 달성 완주 시에만 심플한 `🏆 완주` 축하 배지 표출).
3. **대시보드 헤더 및 홈 위젯 D-0 배지 스마트 숨김 처리**
   - 챌린지 진행 중인 기간(`D-30` ~ `D-1`)에만 `D-Day` 배지를 노출하고, 당월 종료 후 또는 과거 월(8월, 7월 등)을 조회할 때 발생하던 `D-0` 배지를 자동으로 숨겨 불필요한 정보 노출 제거.
4. **최근 3개월 동기간(N일차) 고스트 러너 순위 & 누적 거리 경쟁 (`MonthlyProgress.swift`)**
   - 당월(현재)과 직전 3개월(M-1, M-2, M-3)의 동일 N일차 시점 누적 거리를 추출 및 실시간 랭킹(🥇, 🥈, 🥉, 4위) 정렬.
   - 현재 월의 순위 뱃지(예: `현재 1위`, `현재 2위`) 및 각 월별 누적 거리와 차이값(`+3.2km 앞섬`, `-5.0km 뒤처짐`) 시각화.
   - 지난달 대비 페이스 코칭 메시지(`지난달 대비 +X.Xkm 🔥` 등) 제공.
5. **히스토리 화면 시각성 극대화 (`Views/History/`)**
   - **차트 Y축 Tick Label 제거 (`F-302` ~ `F-305`)**: 불필요한 수치 라벨을 걷어내고 차트 그래프 본연의 영역을 풀와이드로 넓혀 세련되고 시원한 비주얼 완성.
   - **월별 종합 성적표 상하 수직 일렬 칼각 정렬 (`F-306`)**: 거리, 페이스, 심박수, 달린 날 4대 지표를 4분할 균등 너비 컬럼으로 고정하여, 위아래 과월 간에 페이스는 페이스끼리, 심박수는 심박수끼리 정확히 수직 일렬로 정렬되도록 개선.
6. **설정 화면 기능 강화 및 앱 버전 1.2.0 업데이트 (`SettingsView.swift`)**
   - 기본 테마를 **다크 모드** 기본으로 설정.
   - 앱 버전을 **v1.2.0 (1)**로 업데이트.
   - **앱 버전 터치 시 릴리즈 노트 팝업**: 설정 화면에서 '앱 버전'을 터치하면 버전별 업데이트 히스토리를 간편하게 확인할 수 있는 릴리즈 노트 바텀 시트(`ReleaseNotesModalView`) 탑재.
7. **대시보드·캘린더·히스토리 3개 탭 상단 헤더 위치 완벽 일치 & 히스토리 월 선택기 연동**
   - **상단 시작 위치(Y축) 칼각 통일**: 히스토리 탭에도 `NavigationStack`과 `.toolbar(.hidden, for: .navigationBar)`를 동일하게 적용하고 상단 여백(`.padding(.top, 6)`)을 일치시켜, 3개 탭을 전환할 때 헤더 타이틀 높이가 1px의 흔들림 없이 동일 위치에 고정되도록 개선.
   - **히스토리 탭 `< M월 ▾ >` 캡슐 버튼 탑재**: 대시보드·캘린더와 동일한 월 선택기를 히스토리 탭 우측 상단에도 배치하여, 히스토리 화면에서도 월을 즉시 전환하고 전 탭에 실시간 동기화 지원.
8. **히스토리 기간 필터 최적화 ('최근 12개월' 기본 & '올해' 2개 탭 구성, F-301)**
   - 중복 정보였던 '최근 6개월' 옵션을 제거하고, **`최근 12개월` (좌측 기본 선택)**과 **`올해` (우측)** 2개 탭으로 직관적 배치.
   - `올해` 선택 시 당해 연도 1월부터 당월까지의 월별 성장 추이(거리, 페이스, 심박수, 출석률)를 집중 분석할 수 있도록 지원.
9. **바탕화면 전용 Medium(2x4) 위젯 F-102 히어로 카드 일체형 개편**
   - 대시보드의 메인 히어로 카드(F-102)를 그대로 옮겨놓은 듯한 완벽한 시각적 일체감 제공.
   - **좌측**: 오렌지-앰버 그라디언트 100km 프로그레스 링 & 누적 거리(`68.0 km`), 달성률 배지(`68% 달성`).
   - **우측**: 2단 카드 - 이번 달 평균 페이스 카드(`5:30 /km`, 러닝 회수 및 총 시간) & 평균 심박수 카드(`152 bpm`, 심박 강도 가이드).
10. **품질 검증**
   - 14개 핵심 단위 테스트 100% 통과 (`** TEST SUCCEEDED **`).

---

## [v1.2.1] - 2026-09-24 : 1~4번 레인 육상 스타디움 트랙 & 목표 초과 달성 위트 있는 시각화 🏆🏟️

### 🎯 주요 변경 사항
1. **1~4번 레인 육상 스타디움 레이스 트랙 도입 (`RunningTrackProgressCard.swift`)**
   - **올림픽 100m 결승 트랙 모티브**: 단일 라인 트랙을 1~4번 독립 레인으로 전면 개편하여, 마치 육상 경기장에서 과거의 나들과 나란히 레이스를 펼치는 듯한 생동감 구현.
   - **상단 체크포인트 룰러 눈금**: `0k`, `25k`, `50k`, `75k`, `100k` 기준 룰러를 배치하여 직관적인 주행 페이스 가이드 제공.
   - **전 레인 트랙 두께 13pt 균일화**: 1번 레인과 2~4번 레인의 트랙 두께를 13pt(높이 18pt)로 통일하여 시각적 안정감을 부여하고, 당월 레인은 오렌지 테두리 글로우 효과(`Color.orange.opacity(0.35)`)로 세련되게 차별화.
   - **레인별 배정 및 이모지 질주**:
     - `1번 레인 (이번 달 - 현재의 나)`: `9월` (굵은 텍스트) + 골드 그라데이션 트랙 게이지 + 오른쪽 방향 러너(`🏃`, 완주 시 `🎉`)
     - `2~4번 레인 (과거 3개월 - 과거의 나)`: `8월`, `7월`, `6월` (불필요한 레인 번호 제거 후 N월만 간결 표시) + 차분한 모노크롬 슬레이트 그레이 트랙 + 오른쪽 방향 러너(`🏃`)
   - **이모지/텍스트 겹침 100% 해소**: 독립된 레인에서 달리므로 거리가 비슷해도 가려짐 없이 모든 주행 기록이 선명하게 표현됨.
   - **우측 스코어보드 & 줄바꿈 방지**: 각 레인 우측에 누적 거리(폭 70pt, 100km+ 초과 달성 시 줄바꿈 방지 `.lineLimit(1)` 적용, 예: `109.7k`)와 순위 메달(🥇, 🥈, 🥉, 4위는 비움)을 일렬 정돈.
   - **콤팩트 코칭 요약 문구**: `✨ 지난달 대비 +XX.Xkm 🔥` / `-XX.Xkm 🏃` / `지난달과 팽팽한 접전 ⚡️`의 한눈에 들어오는 직관적인 텍스트 제공.
2. **원형 프로그레스 링 `초과달성 중` 배지 도입 (`ProgressRingView.swift`)**
   - 링 게이지는 과도한 이중 링을 배제하고 단일 원형 링을 유지하되, **100%를 초과할 경우 중앙 배지가 SF Symbols 불꽃 심볼이 적용된 `초과달성 중`(`flame.fill`) 앰버 캡슐 배지로 변경**되어 러너의 한계 돌파를 세련되게 강조.
   - 중앙 누적 거리 수치는 실제 초과 달성한 거리(예: `112.5 / 100 km`)를 그대로 정직하게 표기.
3. **레이스 트랙 심플 완주 및 초과 거리 표기 (`RunningTrackProgressCard.swift`)**
   - **헤더 배지 동적 표기**: 초과 달성 시 헤더 우측 배지에 군더더기 없이 깔끔하게 **`🏆 완주 (+12.5km)`** 형태로 초과 거리를 콤팩트하게 안내.
4. **한계 돌파 동기부여 코칭 메시지 고도화 (`MonthlyProgress.swift`)**
   - `+1km ~ +10km`: `"%dkm 완주 후 +%.1fkm 보너스 질주! 한계를 넘어서는 중 🚀"`
   - `+10km 이상`: `"목표를 +%.1fkm 초과 달성! 놀라운 러닝 레전드입니다 🔥"`
   - 과월 조회 시: `"%dkm를 넘어 총 %.1fkm로 한계를 완벽히 돌파했던 달입니다 🏆"`
5. **히스토리 성적표 피드 연동 (`HistoryMonthlyCardView.swift`)**
   - 히스토리 월별 종합 성적표 배지에 `🏆 100km 완주 (+12.5km)` 형태로 초과 달성 거리 보존.
6. **Xcode 캔버스 프리뷰 & 시뮬레이터 테스트 샘플 데이터 지원 (`PreviewSampleData.swift`)**
   - **`ModelContainer.preview` 구축**: `ContentView`, `DashboardView`, `CalendarView`, `HistoryView`의 `#Preview`에 즉시 연동되어, Xcode Canvas 실행 시 당월 112.5km 초과 달성 및 과거 3개월(85k, 72k, 55k)의 생생한 러닝 데이터가 시각화됨.
   - **시뮬레이터 원터치 데이터 생성**: [설정] ➔ [데이터 관리] 섹션에 `테스트 샘플 데이터 채우기 (최근 4개월)` 버튼을 추가하여, 시뮬레이터 실행 시에도 즉시 4개월 치 실전 데이터를 채워볼 수 있도록 지원.
7. **단위 테스트 무결성 검증**
   - 100km 정확 달성 및 초과 달성 케이스 14개 핵심 테스트 100% 통과 (`** TEST SUCCEEDED **`).
8. **캘린더 탭 3분할 통계 콤팩트화 및 'N월 출석체크' 타이틀 개편 (`CalendarView.swift`, `HeatmapGridView.swift`)**
   - **콤팩트 3분할 통계 칩 바**: 텍스트 라벨을 걷어내고 세련된 애플 SF Symbols 벡터 심볼 칩(오렌지 체크 `13일`, 인디고 달 `1일`, 그린 차트 `92.8%`) 형태로 높이를 38pt로 슬림화하여 화면 상단 시야 확보.
   - **'N월 출석체크' 명칭 개편**: 기존 개발자 은어였던 '잔디 심기'를 누구나 한눈에 알아보기 쉬운 `N월 출석체크`로 변경.
   - **세로 간격 최적화**: 캘린더 화면 전반의 간격을 `spacing: 14`로 조율하여 상단 헤더, 출석체크 그리드, 일자별 상세 카드가 자연스럽게 한 화면에 정돈.
9. **캘린더 탭 N월 일별 러닝 거리 막대 차트 신설 (`DailyDistanceChartView.swift`, `F-204`)**
   - **순수 SwiftUI 벡터 기반 일별 러닝 볼륨 시각화**: 1일부터 말일까지 일자별 달린 거리(km)를 슬림 오렌지 막대로 렌더링하고, 휴식일은 2pt 베이스라인 핀으로 전 구간을 끊김 없이 정돈.
   - **인터랙티브 날짜 탭 & 드래그 스크러빙 연동**: 차트 막대를 탭하거나 손가락으로 좌우 드래그하면, 상단 캘린더 및 하단 상세 카드와 즉시 선택 날짜(`selectedDay`)가 햅틱과 함께 동기화.
   - **최고 기록 및 평균 거리 안내**: 이번 달 최고 거리 일자(`👑 최고 12.5km`) 및 달린 날 평균 거리 정보 제공.
   - **배경 가이드 점선**: 50% 및 100% 수평 가이드 점선으로 시각적 척도 제공.

---

## [v1.2.2] - 2026-09-24 : 캘린더 거리x페이스 산점도 차트 도입, Apple Grouped 디자인 시스템 통일 & 위젯 테마 동기화 📊🎨

### 🎯 주요 변경 사항
1. **캘린더 탭 F-204 차트 전면 리팩토링 (월간 거리 × 페이스 산점도 도입, `DailyDistanceChartView.swift`)**
   - **기존 막대 차트의 덜컥거림 원천 해소**: 날짜 선택 시 레이아웃 점프(Layout Shift)가 발생하던 숫자 팝업과 오프셋 슬라이딩을 제거하고, 고정 프레임의 2차원 산점도로 리팩토링.
   - **X축(거리) × Y축(페이스) 시각화**: 한 달 동안 달린 러닝 세션들의 거리(km)와 페이스(분:초) 분포를 한눈에 비교 분석 가능.
   - **X축 1km 단위 정밀 스케일링 & 수직 보조선 그리드**: 기존 5km 단위 올림으로 인해 10.2km만 달려도 15km까지 과도하게 확장되던 현상을 제거하고, 실제 당월 최장 거리에 맞춰 1km 단위 올림(최소 10km 보장, 예: 10.2km -> 11km)으로 타이트하게 밀착. 1km마다 수직 보조선과 2km 간격 라벨을 배치하여 그래프를 가로 전체에 넓고 시원하게 시각화.
   - **Y축 직관성 개선**: 페이스 수치가 높을수록(숫자가 클수록) 상단에, 빠를수록 하단에 위치하도록 정밀 좌표계 보정.
   - **오렌지 단색 투명도 심박수 매핑**: 심박수(bpm)에 비례(0.30~1.00)하여 투명도를 부여함으로써 우측 상단의 심박수 범례(`심박수 ○ → ●`)와 완벽 일치.
   - **부드러운 스프링 인터랙션**: 캘린더 날짜 선택 시 해당 세션 도트가 펄스 링과 함께 즉시 하이라이트되며, 점 직접 터치 시 해당 날짜로 햅틱과 함께 즉각 전환.

2. **앱 전체 4개 탭 디자인 시스템 Apple Grouped 스타일로 통일**
   - 대시보드와 캘린더가 '흰색 배경 + 회색 카드', 히스토리와 설정이 '회색 배경 + 흰색 카드'로 상충되던 불일치를 해결.
   - 4개 탭 전반을 **화면 전체 배경: `systemGroupedBackground` (은은한 연회색)**, **내부 카드: `secondarySystemGroupedBackground` (깨끗한 순백색)**으로 통일.
   - 대시보드 히어로 카드, 레이스 트랙 카드, 최근 기록 카드, 캘린더 잔디 카드, 산점도 카드, 상세 카드가 연회색 배경 위에서 부드러운 그림자와 함께 돋보이는 입체적 룩앤필 완성.

3. **레이스 트랙 카드 디자인 일관성 확보 (`RunningTrackProgressCard.swift`)**
   - 100km 완주 시 노출되던 주황색 외곽 테두리(`.overlay(stroke:)`)를 제거하고, 다른 카드들과 동일하게 은은한 그림자(`.shadow`) 스타일로 일치화.

4. **설정 화면 데이터 관리 정리 (`SettingsView.swift`)**
   - 개발 및 시뮬레이터 테스트용으로 임시 추가되었던 '테스트 샘플 데이터 채우기' 버튼, alert 및 안내 문구를 완전 삭제.
   - 일반 사용자에게 필요한 '기록 데이터 전체 초기화' 기능만 깔끔하게 유지.

5. **홈 화면 위젯 테마(다크/라이트/시스템) 실시간 동기화 (`MediumWidgetView.swift`, `WidgetDataBridge.swift`)**
   - 기기 시스템 모드에만 종속되던 위젯을 개선하여, 앱의 [설정 > 화면 스타일]에서 선택한 테마(`다크 모드`, `라이트 모드`, `시스템 일치`)가 위젯으로 실시간 공유되도록 App Group 파이프라인 구축.
   - 앱이 다크 모드일 때는 위젯도 딥 다크 배경 및 화이트 텍스트로, 라이트 모드일 때는 순백색 카드 배경으로 실시간 적응.

---

## [v1.3.0] - 2026-09-24 : 러닝화 수명 및 소모량 트래킹(Gear Tracker) 시스템 도입 👟🔥

### 🎯 주요 배경 및 기획 의도
- 러너들의 주요 부상(무릎, 족저근막, 신스프린트)은 러닝화 미드솔 쿠션이 수명을 다했을 때 빈번하게 발생함.
- 매 세션마다 어떤 신발을 신었는지 일일이 선택하는 번거로움 없이, **착용 시작일을 기준으로 애플 건강 및 수기 러닝 거리가 완전 자동으로 누적되는 무개입형(Zero-Friction) 트래킹 구조**를 구현.
- 사용자가 복잡한 장비 스펙을 몰라도 되도록, **신발 유형(카테고리) 선택 시 나이키/아식스 등 공식 가이드 기준 권장 수명이 자동으로 세팅**되도록 설계.

### 🚀 구현 완료 내역
1. **러닝화 SwiftData 모델 구축 (`RunningShoe.swift`)**
   - 신발 모델명, 브랜드, 유형(`ShoeType`), 목표 수명 거리(`targetLifespanKm`), 착용 시작일(`startDate`), 기존 누적 거리(`initialDistanceKm`), 주력 여부(`isActive`), 은퇴 여부(`isRetired`) 정의.
   - **유형별 표준 권장 수명 프리셋**:
     - `데일리 쿠션화 / 안정화`: 기본 **600km** (일상 훈련용)
     - `템포 / 스피드 훈련화`: 기본 **500km** (경량 훈련용)
     - `카본 레이싱화`: 기본 **300km** (초경량 대회용)
   - **4단계 건강 상태 엔진 (`ShoeHealthStatus`)**:
     - 🟢 `0% ~ 60% (최상)`: 쿠션과 반발력이 최적의 상태
     - 🟡 `61% ~ 85% (적정)`: 미드솔 마모가 정상 진행 중인 상태
     - 🟠 `86% ~ 100% (교체 준비)`: 쿠션 탄성 저하로 새 신발 준비 권장
     - 🔴 `100% 초과 (수명 완료)`: 미드솔 기능 소진으로 관절 보호를 위한 교체 필수 배지

2. **[추천 1] 설정 탭 > 러닝화 관리 시스템 (`ShoeManagementView.swift`, `AddShoeSheet.swift`, `SettingsView.swift`)**
   - **설정 메뉴 연동**: `목표 러닝` 바로 아래 `장비 관리` 섹션을 신설하여 현재 주력 신발과 소모율(`420km / 600km (70%)`)을 1초 만에 확인.
   - **단일 원스톱 입력 폼 (`AddShoeSheet.swift`)**: 불필요하게 2칸으로 쪼개져 있던 브랜드/모델명 입력을 `러닝화 이름 (예: 나이키 페가수스 41)` 1칸으로 통합하여 입력 번거로움 해소.
   - **원터치 등록/수정 시트 (`AddShoeSheet.swift`)**: 
     - 신발 유형 칩을 누르면 권장 거리가 자동 세팅되며, 슬라이더로 200~1,000km 미세 조정 가능.
     - **정보 수정 버그 완벽 해결**: 수정 버튼 탭 시 신규 등록 시트로 오작동하던 결함을 SwiftUI `sheet(item: $shoeToEdit)` 바인딩 분리 및 명시적 `init`을 적용하여 기존 신발 정보가 100% 온전하게 로드되고 편집되도록 수정.
   - **주력 신발 히어로 카드 & 은퇴 보관함 (`ShoeManagementView.swift`)**:
     - 주력 신발의 대형 누적 거리, 소모율 퍼센트, 게이지 바, 남은 거리 안내 제공.
     - 새 신발 구매 시 [은퇴시키기]를 통해 이전 신발의 최종 누적 거리를 보관함에 영구 보존.

3. **대시보드 히어로 카드 내 하단 듀얼 인디케이터 통합 (`HeroDashboardCard.swift`, `DashboardView.swift`)**
   - **대시보드 세로 공간 극대화**: 별도의 긴 독립 신발 카드로 공간을 차지하는 대신, 메인 히어로 카드 하단 영역에 콤팩트한 칩 형태로 통합.
   - **좌측 스트릭 칩 (`🔥 N일 연속 >`)**: 터치 시 캘린더 화면으로 즉시 전환.
   - **우측 러닝화 소모량 칩 (`👟 신발 이름 70% 🟡` 또는 `👟 러닝화 등록 +`)**: 터치 시 러닝화 관리 시트(`ShoeManagementView`)가 즉시 팝업되어 원터치 확인 및 편집 가능.
   - 별도 카드가 제거됨으로써 대시보드가 한층 더 미니멀하고 단정해졌으며, 핵심 습관(연속 달리기)과 핵심 장비(러닝화 수명)를 한 줄에서 동시 모니터링 가능.

4. **앱 컨테이너 스키마 마이그레이션 (`Run100App.swift`)**
   - `.modelContainer(for: [RunSession.self, RunningShoe.self])`로 멀티 엔티티 컨테이너 안정적 확장.

5. **러닝화 로테이션(2켤레 이상 돌려 신기) 완벽 지원 (`RunSession.swift`, `RunningShoe.swift`, `DayDetailCardView.swift`, `QuickAddModalView.swift`)**
   - **문제 해결**: 조깅용 쿠션화와 포인트 훈련용 카본화/스피드화를 번갈아 신는 러너의 실제 러닝 환경을 고려하여 세션별 착용 신발 독립 매핑 지원.
   - **기본값 자동 지정**: 평소 달릴 때는 현재 설정된 '대표(주력) 러닝화'로 자동 계산되어 번거로움 제로.
   - **캘린더 상세 카드 원터치 신발 변경 (`DayDetailCardView.swift`)**: 캘린더의 일자별 러닝 세션 카드 하단에 `👟 착용 신발 [ 나이키 페가수스 41 ▾ ]` 메뉴를 배치하여 1초 만에 착용 신발 변경 가능.
   - **수동 달리기 기록 모달 (`QuickAddModalView.swift`)**: 오늘 러닝 기록 추가/수정 시에도 착용 신발 드롭다운을 제공하여 원하는 신발로 즉시 저장.
   - **독립적 마일리지 실시간 재계산**: 세션의 신발을 변경하면 양쪽 신발의 누적 주행 거리와 소모율이 실시간으로 각각 정확하게 재계산됨.
